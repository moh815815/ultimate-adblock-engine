// ======================================================
// التعريفات والأنواع - المصطلحات العربية للمطورين
// Arabic Developer Terminology & Type Definitions
// ======================================================

// ---- مصطلحات الاستجابة ----
export const مصطلحات_API = {
  نجاح: "نجاح",
  خطأ: "خطأ",
  تحذير: "تحذير",
  بيانات: "البيانات",
  رسالة: "الرسالة",
  الرمز: "رمز_الحالة",
  الوقت: "وقت_المعالجة_بالميلي_ثانية",
  الصفحة: "رقم_الصفحة",
  الحجم: "حجم_الصفحة",
  المجموع: "إجمالي_النتائج",
} as const;

// ---- رموز الأخطاء بالعربية ----
export const رموز_الأخطاء = {
  مستأجر_غير_موجود: "TENANT_NOT_FOUND",
  مفتاح_API_غير_صالح: "INVALID_API_KEY",
  قائمة_غير_موجودة: "BLOCKLIST_NOT_FOUND",
  قاعدة_غير_صالحة: "INVALID_RULE_PATTERN",
  حد_المعدل_تجاوز: "RATE_LIMIT_EXCEEDED",
  حجم_القائمة_تجاوز: "BLOCKLIST_SIZE_LIMIT_EXCEEDED",
  مزامنة_جارية: "SYNC_ALREADY_IN_PROGRESS",
  مصدر_لا_يمكن_الوصول: "SOURCE_UNREACHABLE",
  تجميع_فاشل: "COMPILATION_FAILED",
  صلاحيات_غير_كافية: "INSUFFICIENT_PERMISSIONS",
} as const;

// ---- أنواع نتائج العمليات ----
export interface نتيجة_عملية<T = unknown> {
  الحالة: "نجاح" | "خطأ" | "تحذير";
  البيانات?: T;
  الرسالة: string;
  رمز_الحالة: number;
  وقت_المعالجة_بالميلي_ثانية: number;
  الطابع_الزمني: string;
}

export interface نتيجة_مُقسَّمة<T> extends نتيجة_عملية<T[]> {
  التقسيم: {
    رقم_الصفحة: number;
    حجم_الصفحة: number;
    إجمالي_النتائج: number;
    إجمالي_الصفحات: number;
    توجد_صفحة_تالية: boolean;
  };
}

// ---- نتائج تحليل الأداء ----
export interface تقرير_أداء_التجميع {
  معرف_العملية: string;
  إحصائيات_الوقت: {
    إجمالي_وقت_التجميع_مللي: number;
    متوسط_وقت_قاعدة_مكرو: number;
    أسرع_قاعدة_مكرو: number;
    أبطأ_قاعدة_مكرو: number;
    انحراف_معياري_مكرو: number;
  };
  إحصائيات_القواعد: {
    إجمالي_القواعد_المُعالجة: number;
    قواعد_شبكة_ناجحة: number;
    قواعد_تجميل_ناجحة: number;
    قواعد_مرفوضة: number;
    نسبة_نجاح_التجميع: number;
  };
  كفاءة_الذاكرة: {
    استهلاك_الذاكرة_قبل_KB: number;
    استهلاك_الذاكرة_بعد_KB: number;
    نسبة_ضغط_القواعد: number;
    تقليل_الحجم_بالمئة: number;
  };
  مخرجات_DNR: {
    قواعد_حجب_شبكة: number;
    قواعد_إعادة_توجيه: number;
    قواعد_تعديل_رأس: number;
    قواعد_تجميل_ترحيل: number;
  };
  تحذيرات: string[];
  تصنيف_الأداء: "ممتاز" | "جيد" | "متوسط" | "ضعيف";
}

// ---- بنية سجل الانحراف ----
export interface سجل_انحراف_شبكي {
  معرف: string;
  الطلب_الأصلي: string;
  النطاق: string;
  نوع_الطلب: string;
  الإجراء_المتخذ: string;
  القاعدة_المطابقة: {
    المعرف: string | null;
    النمط: string | null;
    نوع_القاعدة: string | null;
  };
  التهديد_المكتشف: {
    الفئة: string | null;
    مستوى_الخطورة: "منخفض" | "متوسط" | "عالٍ" | "حرج" | null;
  };
  مقاييس_الأداء: {
    وقت_المعالجة_مكرو: number;
    وفر_النطاق_ترددي_بايت: number | null;
  };
  الخصوصية: {
    عنوان_IP_مُخفى: boolean;
    بيانات_مُجهَّلة: boolean;
  };
  الطابع_الزمني: string;
}

// ---- بيانات إحصائيات الانحراف ----
export interface إحصائيات_الانحراف {
  الفترة_الزمنية: {
    من: string;
    إلى: string;
    المدة_بالساعات: number;
  };
  إجماليات: {
    إجمالي_الطلبات: number;
    طلبات_محجوبة: number;
    طلبات_مُحوَّلة: number;
    طلبات_مسموح_بها: number;
    معدل_الحجب_بالمئة: number;
  };
  توفير_الموارد: {
    إجمالي_النطاق_الترددي_المحفوظ_MB: number;
    تقدير_وقت_التحميل_المحفوظ_ثانية: number;
    تتبعات_موقوفة: number;
  };
  تصنيف_التهديدات: Record<string, number>;
  أكثر_النطاقات_محجوبة: Array<{ النطاق: string; العدد: number }>;
  أداء_محرك_الحجب: {
    متوسط_وقت_معالجة_مكرو: number;
    الحد_الأقصى_مكرو: number;
    إجمالي_الطلبات_في_الثانية: number;
  };
}

// ---- بيانات المزامنة ----
export interface نتيجة_مزامنة_قائمة {
  معرف_المزامنة: string;
  معرف_القائمة: string;
  اسم_القائمة: string;
  حالة_المزامنة: "مكتملة" | "فاشلة" | "ملغاة";
  تفاصيل_التغييرات: {
    قواعد_مضافة: number;
    قواعد_محذوفة: number;
    قواعد_معدلة: number;
    الإجمالي_قبل: number;
    الإجمالي_بعد: number;
    نسبة_التغيير: number;
  };
  مقاييس_الأداء: {
    وقت_التنزيل_مللي: number;
    وقت_التجميع_مللي: number;
    إجمالي_الوقت_مللي: number;
  };
  المصدر: {
    الرابط: string;
    ETag: string | null;
    إصدار_EasyList: string | null;
  };
  خطأ?: string;
  الطابع_الزمني: string;
}

// ---- طلب تشغيل المزامنة ----
export interface طلب_مزامنة {
  معرفات_القوائم?: string[];
  إجبار_إعادة_التجميع?: boolean;
  الأولوية?: "عادية" | "عالية" | "عاجلة";
}

// ---- طلب إنشاء قائمة حجب ----
export interface طلب_إنشاء_قائمة {
  الاسم: string;
  الوصف?: string;
  رابط_المصدر: string;
  النوع: "EasyList" | "EasyPrivacy" | "EasyListArabic" | "uBlockOrigin" | "AdGuard" | "مخصصة";
  تكرار_المزامنة_ثانية?: number;
}

// ---- طلب تسجيل انحراف ----
export interface طلب_تسجيل_انحراف {
  الطلبات: Array<{
    الطلب_الأصلي: string;
    النطاق: string;
    نوع_الطلب: string;
    الإجراء: string;
    معرف_القاعدة?: string;
    نمط_القاعدة?: string;
    فئة_التهديد?: string;
    وقت_المعالجة_مكرو: number;
    وفر_النطاق_ترددي_بايت?: number;
  }>;
}
